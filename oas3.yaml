openapi: 3.0.0
info: 
    version: '3.0'
    title: "Prova API"
    description: "provo a scrivere la api"
    license: 
      name: MIT
servers: 
  - url: http://localhost:3000/api/v1
    description: LocalHost
paths: 
  /users: 
    post: 
      description: "Creates a new user in the system"
      summary: Create a new user
      requestBody:
        content: 
          application/json:
            schema:
              type: object
              required: 
                - password
                - email
              properties: 
                password:
                  type: string
                  description: 'Password dello user'
                email: 
                  type: string
                  description: 'Email dello user'
      responses:
        '201':
          description: 'The created user'
          headers: 
            Location:
              schema:
                type: string
              description: Link to the newly created user
              
    get: 
      description: "Get all the users in the system"
      summary: Get all users
      parameters:
      responses:
        '200':
          description: "All the users"
          #content: 
            #application/json:
              #schema: 
                #$ref: '#/components/schemas/User'
                
    patch:
      description: "Updates the user's email"
      summary: Update email
      parameters:
        - name: id
          in: path
        - name: email
          in: body
      responses:
        '200':
          description: "Email updated"
          
    delete:
      description: "Delete a user"
      summary: Delete user
      parameters:
        - name: id
          in: path
      responses:
        '200':
          description: "User deleted"

  /users/{id}:    
    get:
      description: >-
        Gets information about a specific user, identiied by an ID
      summary: Get a user
      parameters:
        - name: id
          in: path
          required: true
          description: 'ID of the requested user'
      responses: 
        '200':
          description: 'The user corresponding to the input ID'
          #content:
            #application/json:
              #schema:
                #$ref: '#/components/schemas/User'

  /users/{email}:    
    get:
      description: >-
        Gets information about a specific user, identified by an email
      summary: Get a user
      parameters:
        - name: email
          in: path
          required: true
          description: 'email of the requested user'
      responses: 
        '200':
          description: 'The user corresponding to the input email'
          #content:
            #application/json:
              #schema:
                #$ref: '#/components/schemas/User'  

  /meteoComponents:  
    get:
      description: >-
        Gets the list of all meteo components saved.
      summary: View all meteos components that are part of an itinerario
      responses:
        '201':
          description: 'Collection of meteoComponents'
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/MeteoComponent'
    post:
      description: >-
        Creates a new meteoComponent, >-
        which is the set of the informations about the >-
        meteo in a stage of an itinerario
      summary: Create meteoComponent
      requestBody:
        content: 
          application/json:
            schema:
              type: object
              required: 
                - date
                - cityName
              properties: 
                temp_Max:
                  type: string
                  description: 'Maximum temperature'
                temp_Min: 
                  type: string
                  description: 'Minimum temperature'
                date:
                  type: string
                  description: 'Date'
                cityName:
                  type: string
                  description: 'Name of the city'
      responses:
        '201':
          description: 'The created user'
          headers: 
            Location:
              schema:
                type: string
              description: Link to the newly created user
  
  /meteoComponents/{id}:    
    get:
      description: >-
        Gets information about a specific meteoComponent
      summary: Get a specific meteoComponent
      parameters:
        - name: id
          in: path
          required: true
          description: 'Id of the requested meteoComponent'
      responses: 
        '200':
          description: 'The user corresponding to the input email'
          #content:
            #application/json:
              #schema:
                #$ref: '#/components/schemas/User'
                
  /meteos:
    get:
      description: >-
        Gets the current meteo.
      summary: View current meteo
      responses: 
        '200':
          description: 'Collection of books'
          content:
            application/json:
              schema: 
                type: array
                items: 
                  $ref: '#/components/schemas/Meteo'
    
  /meteos/current/{city}:
    get: 
      description: >-
        Gets information about current meteo of a city
      summary: Get meteo
      parameters: 
        - name: city
          in: path
          required: true
          description: 'Name of the requested city'
      responses:
        '200':
          description: 'The meteo corresponding the city'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Meteo'

  /itineraries:
    get:
      description: >-
        Gets the list of all the itineraries
      summary: Get itineraries
      responses:
        '200':
          description: 'All the itineraries of all the users'
          content:
            application/json:
              schema: 
    post:
      description: >-
        Creates a new itinerary 
      summary: Creates itinerary
      requestBody:
        content:
          application/json:
            schema:
              type: object
              properties:
                user_id:
                  type: string
                  description: 'ID of the user who made the itinerary'
      responses:
        '200':
          description: 'Itinerary saved successfully.'
   

components:
  schemas:
    User:
      type: object
      required:
        - id
        - email
        - password
      properties:
        id: 
          type: string
          description: 'ID of the user'
        email:
          type: string
          description: 'email of the user'
        password: 
          type: string
          description: 'password dello user'
    MeteoComponent:
      type: object
      required: 
        - date
        - temp_Min
        - temp_Max
      properties:
        date: 
          type: string
          description: 'Data del meteo'
        temp_Min:
          type: string
          description: 'temperatura minima di quel giorno in tal posto'
        temp_Max:
          type: string
          description: 'temperatura massima di quel giorno in tal posto'
        cityName: 
          type: string
          description: 'il nome della città di cui si vuole il meteo'
    Meteo:
      type: object
      required:
        - temperatura
        - data
      properties: 
        temperatura: 
          type: string
          description: "prova_uno"
        data: 
          type: string
          description: "prova_uno"